
import { configureStore } from '@reduxjs/toolkit';

// Create a simple store for now - the cryptoSlice will be added when needed
export default configureStore({
    reducer: {
        // Add reducers here as needed
    },
});
